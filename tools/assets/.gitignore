# Downloaded / produced artifacts — never committed.
/game/*
/audio/*
/output/*
/base/*
/Z3/
*.zip

# Music pvms.bat fetches from SUINEVERE_MUSIC_URL and caches here (with a .part
# sibling mid-fetch). Not committed: a third-party recording the disc pulls at
# build time, exactly like the CD-DA audio above.
/music/caltheme.wav
/music/*.part
!Zaturn - Complete (USA) (Netlink Edition)/.gitkeep
**/*.bin

# Stray artifact from the old games.bat quoting bug; keep it from coming back.
Matched
